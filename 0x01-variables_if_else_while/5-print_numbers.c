#include <stdio.h>

/**
* main - Accept empty value
* write - Screen information
* @void: Description of a empty parameter
* Description: This sort number from 9 to 1
* Return: 0 Value if it works
*/

int main(void)

{

	int x;

	for (x = 0; x < 10; x++)

	{
		printf("%d", x);
	}
	putchar('\n');
	return (0);
}
